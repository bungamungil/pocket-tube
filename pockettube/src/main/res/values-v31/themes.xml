<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pockettube" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@android:color/system_accent1_500</item>
        <item name="colorPrimaryDark">@android:color/system_accent1_600</item>
        <item name="colorAccent">@android:color/system_accent2_500</item>
        <item name="android:colorBackground">@android:color/system_neutral1_10</item>
    </style>

    <style name="Theme.Pockettube" parent="Base.Theme.Pockettube" />

    <style name="Theme.Pockettube.Toolbar" parent="Theme.Pockettube">
        <item name="colorControlNormal">@color/white</item>
    </style>
</resources>